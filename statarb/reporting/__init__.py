"""Notes, tables, and figures. Every deliverable ships with regenerable output.

The standing requirement across SPEC Part D is that every number in every note is
regenerable from raw under a recorded config hash. This subpackage is where that promise
is kept: a note is generated from the artifacts, never hand assembled from numbers pasted
out of a notebook.

Outputs: the 3 cointegration notes (D6), the break catalogue (D9), the engine note (D14),
the 4 corridor charts (D15), the attribution tables (D17), and the strategy note (D18).

Claims rule on anything that leaves this repo: a number is quotable only once its
acceptance gate has passed and it regenerates. Work in progress is described without
numbers.
"""
